'use strict'
/**
 * minimum : function two compares two numbers
 * @param {int} num1 : first number to be compared
 * @param {int} num2 : second number to be compared
 * return: the minimum number
 */
const minimum = (num1, num2) => {return num1 < num2 ? num1 : num2;}
